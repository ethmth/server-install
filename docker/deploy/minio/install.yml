install:
  name: minio
  location: $HOME
  autostart: true
  volumes:
  - minio_data
  files:
  - compose.yml
  messages:
  - "Set minio MINIO_ACCESS_KEY, MINIO_SECRET_KEY, MINIO_SERVER_URL, MINIO_BROWSER_REDIRECT_URL in compose.yml"
